
package lostinbabuland;

import javafx.stage.Stage;

public class MemoryCardGame implements MiniGame {
    @Override public void start(Stage stage) { new PlatformerSprint().start(stage); }
}
//somehow useless again